# Translation Tools Requirements

# PDF page extraction as images
pdf2image>=1.16.0
# Note: Also requires poppler system package:
#   Ubuntu/Debian: sudo apt-get install poppler-utils
#   macOS: brew install poppler

# JSON processing
jsonschema>=4.0.0

# Text processing
chardet>=5.0.0
regex>=2023.0.0

# Optional: PDF generation automation
# (XeLaTeX handles actual PDF creation)
Jinja2>=3.0.0
